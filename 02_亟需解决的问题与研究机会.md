# 长程智能体领域亟需解决的问题与研究机会

> 关于不同场景的数据采集/合成、质量校验和训练，详见 [专题报告](05_场景数据合成_质量判准与训练路线.md)。该报告用具体前作收窄了下面的宏观问题：仓库已有多接口、跨天事件、judge 人审、动态 rubric 和恢复训练，研究机会在这些能力的具体边界，不能仅凭目录数量认定空白。

## 结论先行

综述把长程能力定义为**在环境反馈下持续完成相互依赖的决策**，并用 `Agent = πθ ⊕ H` 强调模型和运行时 harness 的耦合。仓库的 775 条条目显示，研究已经覆盖规划、记忆、工具、编排、验证、环境合成、Agentic RL、OPD、自演化和多种应用；但条目数量并不等于问题已解决。当前最紧迫的瓶颈集中在五件事：

1. 能否定义并测量真正的长程成功，同时区分模型、harness、工具和环境的贡献。
2. 能否让跨窗口、跨会话的状态保持真实、可追溯、可失效和可恢复。
3. 能否在真实副作用与稀疏反馈下及时发现错误、分配信用并恢复目标。
4. 能否在固定金钱、延迟、token 和动作预算内稳定完成任务。
5. 能否把经验迁移和自我改进做成可审计、可回滚、不会削弱安全约束的过程。

下面的 P0/P1 不是论文原文的官方排序，而是根据综述的四条 frontier 主线、代表工作讨论的失败模式和落地风险作出的研究优先级判断。条目密度描述目录组织，不能作为问题未解决的证据。

## P0：需要优先投入的基础问题

### P0-1 统一的长程定义、评测和贡献归因

**问题。** 目前“长”常被等同于 token 数、最大步数或运行时长。综述区分了 H1（单上下文交互）、H2（跨上下文/会话状态）和 H3（跨任务经验积累），并指出 METR 的 time horizon 是固定成功率下的人类专家任务时长代理。软件工程、网页、桌面、移动端、深搜、多模态和具身 benchmark 的终止条件、工具权限、反馈密度各不相同，分数不能直接横向比较。更严重的是，换一个模型、提示、记忆、工具 schema 或 verifier，测到的是不同的 `πθ ⊕ H`。

**仓库证据。** `Benchmarks and Resources` 小节包含 38 条，但其他章节也列有 benchmark，不能用它与 `Applications` 的 226 条比较评测研究规模。代表资源包括 SWE-Bench Pro、Terminal-Bench、WideSearch、DeepResearch Bench、AndroidWorld、τ-bench、YC-Bench；HORIZON 等已做跨域诊断。剩余问题应依据具体评测的覆盖范围判断，详见专题报告第 5 节。仓库机械解析为 775 条、739 个 URL 主键和 726 个规范化标题，仅描述固定快照。

**需要解决的研究任务。** 建立包含 H1/H2/H3 的任务协议：显式依赖图深度、跨会话边界、反馈延迟、可逆/不可逆动作、工具故障率、预算和成功率；同时固定任务集和预算，交叉替换模型与 harness，报告交互项而非只报单一总分。

**可检验假设与指标。** 在相同模型和工具下，仅改变记忆/验证模块，若 H2 成功率、错误恢复率和证据完整率显著变化，才能归因于 harness；报告 success@budget、首次错误步、恢复步数、跨会话状态保真度、校准误差和 95% 置信区间。METR 的时长只能作为一个维度，不能作为“无人值守稳定工作时长”的替代物。

### P0-2 可验证的状态与记忆：从“存储历史”到“保持真相”

**问题。** 历史摘要、向量检索和记忆库会删除细节、混入过期事实、掩盖冲突或把错误经验复用到新任务。H2/H3 需要记忆写入、更新、失效、冲突处理、来源追踪和权限隔离；仅测“能否回忆旧事实”不能证明记忆改善了后续动作。

**仓库证据。** Context and Memory 子节有 43 条，近年同时出现 ReSum、MEM1、MemAgent、ACE、Memory-as-Action、Memex(RL)、ContextBudget、[Memory-R2](https://arxiv.org/abs/2605.21768)、[MemoryArena](https://arxiv.org/abs/2602.16313)、EverMemOS 等，研究从摘要压缩扩展到可学习的记忆读写和索引回溯，但也反复承认压缩损失、注意力稀释、预算约束和记忆状态改变训练环境的问题。

**需要解决的研究任务。** 设计带 provenance 的状态表示：每条记忆记录来源、时间、适用范围、置信度、失效条件和产生它的工具观测；支持“撤回/更正/隔离”而非只追加。把工作记忆（当前子目标和约束）与经验记忆（策略和失败教训）分开评估，并测试恶意或错误记忆注入。

**可检验假设与指标。** 在多会话任务中注入过期、冲突和恶意记忆，比较成功率、错误复用率、冲突检测 F1、证据可追溯率、记忆读写 token 成本和恢复时间；要求系统能回答“这条决策依赖哪次观测”，并在源状态变化后自动降权或撤回。

### P0-3 真实环境交互、工具副作用与 verifier 可靠性

**问题。** 模拟环境便宜但可能默认工具永远成功、没有延迟和副作用；真实环境昂贵、慢且不可逆。测试通过、页面显示成功或多个模型意见一致，都不保证目标状态真的达成。checker 自身还可能有盲点，导致“验证了错误”。

**仓库证据。** Data and Environment Synthesis、Verification、Tools/MCP 相关条目密集出现 [AutoWebWorld](https://arxiv.org/abs/2602.14296)、[Agent-World](https://arxiv.org/abs/2604.18292)、[WebWorld](https://arxiv.org/abs/2602.14721)、[SWE-Gym](https://arxiv.org/abs/2412.21139)、MCPVerse/MCPMark、AgentRewardBench、Verifiable Process Rewards、Schema First Tool APIs 等；Frontiers 明确把 real-world environment interaction 列为主线。仓库同时收录 AgentSpec、AgentBound、AEGIS、Before the Tool Call 等执行边界和前置授权工作，反映工具副作用已成为核心问题。

**需要解决的研究任务。** 建立“可验证环境 + 真实回放 + 小规模真实试验”的分层体系，记录状态转移、延迟、失败语义和副作用；让 verifier 检查环境后置条件、证据链和安全不变量，而不是只评价最终文本。对 verifier 做独立金标准、对抗测试和失效传播分析。

**可检验假设与指标。** 报告模拟到真实的性能落差、工具失败恢复率、后置条件满足率、verifier precision/recall、误放行/误阻断率、不可逆动作前拦截率；任何“形式保证”都必须写明被编码的规则、可观察状态和拦截覆盖范围。

### P0-4 早期错误检测、恢复和目标保持

**问题。** 长轨迹中一个错误工具调用、错误完成标记或过时计划会污染后续步骤，最后才暴露失败；自我反思可能只是重复原判断，错误还会演变成目标漂移。长程系统需要知道何时暂停、回滚、换路、求助或重新澄清目标。

**仓库证据。** 综述 Frontier 将 reflection & error robustness 单列为方向；仓库收录 [Reflexion](https://arxiv.org/abs/2303.11366)、[Self-Refine](https://arxiv.org/abs/2303.17651)、Sherlock、MiroThinker verification、TrajAD、ProbGuard、Agentic Uncertainty Quantification、Hindsight Credit Assignment 等。Loops and Workflows 还同时列出线性、计划执行和树搜索，说明核心差异在失败后从哪里继续。

**需要解决的研究任务。** 把 checkpoint、可逆操作、子目标完成证明、异常检测、重规划和人类升级设计成统一恢复协议；训练恢复策略覆盖“错误前缀”，而不是只提供专家成功轨迹。目标状态应由外部事实和用户约束共同定义，并持续检查是否漂移。

**可检验假设与指标。** 以相同故障注入率比较平均恢复成本、错误传播长度、目标漂移率、求助校准（选择性风险）、回滚成功率和最终效用；把“发现错误”与“采取有效恢复动作”分开计分。

### P0-5 动态状态下的信用分配和 Agentic RL 稳定性

**问题。** 长程奖励稀疏且延迟；记忆写入、工具副作用和环境更新会使不同 rollout 处于不同有效状态，直接做组相对比较并不公平。盲目增加 step-level dense reward 又会放大稀有幸运动作、噪声锚点和奖励投机。

**仓库证据。** Optimization 子节有 246 条，其中 Agentic Reinforcement Learning 74 条、OPD 17 条。近年条目包括 Memory-R2（针对不同记忆状态的公平比较）、ECPO（证据校准的 step credit）、Turn-PPO、StepPO、TCOD、SOD、f-OPD、Hindsight Credit Assignment、AgentRewardBench。

**需要解决的研究任务。** 以状态版本和记忆快照为单位进行局部重采样；联合 outcome、process、rubric 和安全奖励并校准不确定性；区分 token、turn、step、subgoal 四种更新粒度。训练数据应覆盖失败恢复和 verifier 误差。

**可检验假设与指标。** 测量 advantage 方差、不同 rollout 的状态可比性、训练振荡、成功率尾部、奖励与真实后置条件的相关性；在固定环境预算下比较样本效率和跨 horizon 泛化，禁止只报告一次性最高分。

## P1：紧随其后的系统性问题

### P1-1 成本与预算感知的规划

长程任务的 token、工具调用、并行分支、浏览器/云资源和人工审批成本都可能成为硬约束。当前许多方法先生成计划再超预算，缺乏校准的“继续、压缩、换模型、并行还是停止”决策。[ContextBudget](https://arxiv.org/abs/2604.01664)、Inference-Time Budget Control、[RouteLLM](https://arxiv.org/abs/2406.18665)、Flash-Searcher 等工作说明问题正在形成，但尚无统一的预算—成功率—风险曲线。

应把预算作为状态变量和训练目标，报告 success/cost、延迟、峰值上下文、工具费用、人工介入次数和超预算率；比较固定预算下的自适应路由，而不是无限 test-time scaling。

### P1-2 Harness 可迁移性与工具/Agent 协议互操作

同一模型可能绑定某一套消息格式、工具 schema、记忆 API 或角色拓扑，换 harness 后性能和排名大幅变化。MCP、A2A、ACP、Agent Protocol、AgentSpec 等条目快速增加，但协议标准化不等于语义、权限、错误和状态的一致。

应定义最小可移植接口：观测、动作、状态版本、错误语义、权限、取消、checkpoint 和 verifier 结果；用多模型 × 多 harness × 多环境矩阵报告迁移损失、协议适配成本和工具发现错误率。

### P1-3 安全的持续学习、自演化与经验治理

外部经验可能被提示注入或污染；自演化可能优化 benchmark 漏洞、扩大权限、遗忘旧约束或把不安全技能传播给后续任务。[DGM](https://arxiv.org/abs/2505.22954) 证明代码/工作流可以通过迭代验证改进，但它不等于权重层面的安全终身学习。

应把经验写入设为受控提交：来源审计、沙箱验证、能力/权限分级、回滚、版本比较和不变量检查；分别测同分布收益、未见任务泛化、旧任务保持率、约束违反率和恶意经验传播率。任何自修改都需要独立 verifier 和人工升级路径。

### P1-4 多模态与具身的多时间尺度 grounding

GUI、视频、音频和机器人反馈具有不同采样率、延迟和噪声；“把视觉当作额外工具”会丢失空间、时间和因果信息。具身系统还要同时处理慢规划、快控制、物理约束和不可逆动作。

仓库的 Multimodal Agents 有 44 条，含 OmniGAIA、[Video-MME](https://arxiv.org/abs/2405.21075)、[LongVideoBench](https://github.com/longvideobench/LongVideoBench)、StreamingBench、OmniVideoBench、[EmbodiedBench](https://arxiv.org/abs/2502.09560)、WorldModelBench；具身和 world-model 条目大量出现在应用与环境合成部分。应采用原生多模态状态、时间对齐和动作条件世界模型，报告跨模态证据保留、定位误差、延迟鲁棒性、仿真到真实落差和安全停止率。

### P1-5 多智能体协作的通信、信用与安全边界

增加 agent 数量不必然增加能力：任务划分、共享对象版本、写入权、合并冲突、重复验证和消息成本可能抵消并行收益。中心、层级、对等和自组织拓扑在不同任务上各有适用条件。

应把子任务独立可验收性、通信 token、冲突率、重复工作率、单点失效和恶意 agent 隔离纳入指标；比较单 agent + skills 与多 agent 的 Pareto 前沿，而不是只展示角色数量增加后的总分。

## 研究路线建议

### 路线 A：可审计的 H2 记忆与恢复基准

选取 MemoryArena、τ-bench、AppWorld 或 YC-Bench 风格的多会话任务，加入过期/冲突记忆、工具延迟、部分失败和用户目标变更。固定基础模型与工具，比较摘要、索引回溯、记忆作为动作和带 provenance 的记忆。主要产出是状态保真、冲突处理、错误恢复和成本的联合曲线。

### 路线 B：Verifier—Hook 闭环

在 SWE、浏览器或 CLI 环境中定义可执行后置条件和安全不变量，分别测步骤级 hook、终局 verifier、独立 checker、模型自评和组合策略。注入 verifier 漏洞与工具副作用，分析误放行如何传播到最终结果。主要产出是 verifier 的校准与失效模式，而非又一个总分。

### 路线 C：预算约束下的动态信用分配

以固定 token/工具/时间预算训练和测试 [Memory-R2](https://arxiv.org/abs/2605.21768)/[ECPO](https://arxiv.org/abs/2606.05885)/turn-level/OPD 类方法，记录状态可比性、advantage 方差、恢复样本效率和跨 horizon 泛化。把奖励投机、幸运动作和全错组作为标准压力测试。

### 路线 D：跨 harness 迁移与安全自演化

建立统一观测—动作—状态—权限协议，在至少三套 harness、两种基础模型和两个应用域上交换工作流与经验；加入沙箱、回滚和独立不变量 checker，测迁移损失、泛化、旧能力保持和安全退化。该路线直接对应综述指出的 harness transferability、continual learning、self-evolving agent 三个 frontier。

## 判断一项工作是否真正推进了长程能力

- 是否增加了**必要的决策依赖**，而不只是拼接更多无关步骤？
- 是否公开了模型、harness、工具、环境、预算和 verifier 配置？
- 是否测了跨上下文/跨任务泛化、失败恢复和成本，而不只报最终成功率？
- 是否能追溯每个关键决策依赖的观测和记忆来源？
- 是否测试了工具失败、延迟、提示注入、记忆污染、目标变更和 verifier 失效？
- 是否区分了外部 harness 改进、自修改代码、参数更新和真正的经验迁移？

这些条件满足得越多，结果越接近可部署的长程能力，而不是一次性提示技巧或封闭环境中的局部优化。
